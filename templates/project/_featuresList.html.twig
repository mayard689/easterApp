<i class='btn btn-primary fas fa-plus-circle float-right d-flex align-items-center flex-row-reverse' for="project_addFeature">
    {{ form_widget(form.addFeature, {'attr': {'class': 'pb-0 pt-0 btn btn-primary'}}) }}
</i>

<table class="table table-hover feature-list">

    <thead>
    <tr>
        <th scope="col">Nom de la fonctionnalité</th>
        <th class="text-center" scope="col">Catégorie de la fonctionnalité</th>
        <th class="text-center" scope="col">Charge estimée</th>
        <th class="text-center" scope="col"></th>
    </tr>
    </thead>
    <tbody>


    {% for featureCategory in featureCategories??{} %}
        <tr>
            <td colspan="4" class="feature-category w-50">
                <div><a class="collapse-toggler" data-toggle="collapse" href="#collapseDescription{{ featureCategory.id }}" aria-expanded="true" aria-controls="collapseDescription{{ featureCategory.id }}">
                    Catégorie : {{ featureCategory.name }}
                </a></div>
            </td>

        </tr>

        {% for projectFeature in form.projectFeatures %}
            {% if featureCategory.id == projectFeature.vars.data.category.id %}
                {{ include('project/_featureLine.html.twig') }}
            {%  endif %}
        {% endfor %}
    {% else %}
        <td>Pas de fonctionnalité enregistrées !</td>
    {% endfor %}

    </tbody>
</table>
