{% extends 'base.html.twig' %}

{% block body %}

    <div class="text-center error-content">

        <div class="error">
            <h1 class="h1">Big Bill</h1>
            {% block errorText %}{% endblock %}
            <a class="btn btn-primary" href="{{ path('project_index') }}">Revenir à l'accueil</a>
        </div>

    </div>

{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('errors') }}
{% endblock %}
