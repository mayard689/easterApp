<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>{% block title %}Big Bill | Change Password{% endblock %}</title>
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
        {{ encore_entry_link_tags('security') }}
    {% endblock %}
</head>
<body>
<main class="main">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>Big Bill</h1>
            </div>
        </div>
        <div class="row align-items-center justify-content-center">
            <div class="col-md-6 col-md-offset-4">
                <div class="form-card card">
                    <div class="card-body">
                        <div class="text-center">
                            <img src="{{ asset('build/img/lock.png') }}" alt="car-key">
                            <h2 class="text-center pt-3 pb-2">Saisissez votre mot de passe</h2>
                            <p class="pb-2">Le mot de passe doit contenir 8 caractères, dont 1
                                chiffre, 1 majuscule, et un caractère spécial.
                            </p>
                            {{ form_start(form) }}
                            <div class="row form-group">
                                {{ form_label(form.plainPassword.first) }}
                                <div class="input-group pt-2">
                                    {{ form_widget(form.plainPassword.first) }}
                                    <div class="input-group-append">
                                        <button class="btn btn-outline-primary" type="button"><i class="fas fa-eye"></i></button>
                                    </div>
                                </div>
                            </div>
                            <div class="row form-group">
                                {{ form_label(form.plainPassword.second) }}
                                <div class="input-group pt-2">
                                    {{ form_widget(form.plainPassword.second) }}
                                    <div class="input-group-append">
                                        <button class="btn btn-outline-primary" type="button"><i class="fas fa-eye"></i></button>
                                    </div>
                                </div>
                            </div>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
<script src="https://kit.fontawesome.com/bfe9f51c30.js" crossorigin="anonymous"></script>
</body>
</html>
